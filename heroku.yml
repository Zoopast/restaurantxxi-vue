setup:
  config:
    env_file: .env
build:
  docker:
    release:
       dockerfile: Dockerfile
       command:
        - ./docker-vite.sh
        - ./docker-rails.sh
    web: Dockerfile
run:
  web: bundle exec puma -C config/puma.rb